% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mirBottleneck_project.R
\name{run_mirBottleneck_project}
\alias{run_mirBottleneck_project}
\title{Run mirBottleneck on a real project (RDS inputs)}
\usage{
run_mirBottleneck_project(
  mirna_log_rds,
  rna_rds,
  clinical_rds,
  mirna_norm_map_rds,
  out_dir,
  mirna_targets_rds = NULL,
  query_network = TRUE,
  batch_size = 50,
  min_targets = 5,
  rescue_mirnas = c("hsa-miR-21-5p", "hsa-miR-21-3p", "hsa-miR-155-5p", "hsa-miR-155-3p",
    "hsa-miR-196a-5p", "hsa-miR-196b-5p", "hsa-miR-210-3p"),
  vss_keep_top = 300,
  n_perm = 200,
  max_targets = 200,
  cox_p_threshold = 0.15,
  report = FALSE,
  cohort_name = "TCGA-PAAD"
)
}
\arguments{
\item{mirna_log_rds}{Path to \code{.rds} containing \code{mirna_log}}

\item{rna_rds}{Path to \code{.rds} containing \code{rna_sym}}

\item{clinical_rds}{Path to \code{.rds} containing \code{clinical_df}}

\item{mirna_norm_map_rds}{Path to \code{.rds} containing \code{mirna_norm_map}}

\item{out_dir}{Output directory (created if needed)}

\item{mirna_targets_rds}{Optional path to \code{.rds} containing \code{mirna_targets}
as returned by \code{build_network()} (columns: mirna, targets(list), n_targets)}

\item{query_network}{If TRUE and \code{mirna_targets_rds} is NULL, build network online.}

\item{batch_size}{Passed to \code{build_network()}}

\item{min_targets}{Passed to \code{build_network()}}

\item{rescue_mirnas}{Passed to \code{build_network()}}

\item{vss_keep_top}{Integer, keep top-N miRNAs by VSS for coherence scoring.}

\item{n_perm}{Passed to \code{score_coherence()}}

\item{max_targets}{Passed to \code{score_coherence()}}

\item{cox_p_threshold}{p-value threshold for composite_score().}

\item{report}{Logical. If TRUE, generate an HTML report.}

\item{cohort_name}{Character label used in the HTML report.}
}
\value{
Invisible list of key objects.
}
\description{
Reads user-provided \code{.rds} objects (no demos) and writes results to \code{out_dir}.
Supports either a user-supplied miRNA-target network (offline deterministic)
or online network building via \code{multiMiR} (miRTarBase) through \code{build_network()}.
}
\details{
Expected objects:
\itemize{
\item mirna_log: numeric matrix, miRNAs x patients (rownames miRNA IDs, colnames patient IDs)
\item rna_sym: numeric matrix, genes x patients (rownames gene symbols, colnames patient IDs)
\item clinical_df: data.frame with patient, OS_days, OS_status, age, stage_clean
\item mirna_norm_map: data.frame with original, norm
}
}
\examples{
set.seed(1)
n_pat <- 12
pat_ids <- paste0("P", 1:n_pat)

mirna_log <- matrix(rnorm(4 * n_pat), nrow = 4)
rownames(mirna_log) <- c("hsa-miR-21-5p", "hsa-miR-155-5p", "hsa-miR-1-3p", "hsa-miR-2-3p")
colnames(mirna_log) <- pat_ids

rna_sym <- matrix(rnorm(6 * n_pat), nrow = 6)
rownames(rna_sym) <- c("KRAS", "TP53", "SMAD4", "CDKN2A", "EGFR", "BRCA1")
colnames(rna_sym) <- pat_ids

clinical_df <- data.frame(
  patient     = pat_ids,
  OS_days     = round(rexp(n_pat, rate = 0.001)),
  OS_status   = sample(0:1, n_pat, replace = TRUE),
  age         = sample(45:75, n_pat, replace = TRUE),
  stage_clean = sample(c("I", "II", "III"), n_pat, replace = TRUE)
)

mirna_norm_map <- data.frame(
  original = rownames(mirna_log),
  norm = rownames(mirna_log)
)

mirna_targets <- data.frame(
  mirna = c("hsa-miR-21-5p", "hsa-miR-155-5p"),
  targets = I(list(c("KRAS", "TP53"), c("EGFR", "BRCA1"))),
  n_targets = c(2L, 2L)
)

tmp <- tempfile("mirBottleneck_")
dir.create(tmp)
mirna_log_rds <- file.path(tmp, "mirna_log.rds")
rna_rds <- file.path(tmp, "rna_sym.rds")
clinical_rds <- file.path(tmp, "clinical.rds")
mirna_norm_map_rds <- file.path(tmp, "mirna_norm_map.rds")
mirna_targets_rds <- file.path(tmp, "mirna_targets.rds")

saveRDS(mirna_log, mirna_log_rds)
saveRDS(rna_sym, rna_rds)
saveRDS(clinical_df, clinical_rds)
saveRDS(mirna_norm_map, mirna_norm_map_rds)
saveRDS(mirna_targets, mirna_targets_rds)

run_mirBottleneck_project(
  mirna_log_rds = mirna_log_rds,
  rna_rds = rna_rds,
  clinical_rds = clinical_rds,
  mirna_norm_map_rds = mirna_norm_map_rds,
  out_dir = tmp,
  mirna_targets_rds = mirna_targets_rds,
  query_network = FALSE,
  n_perm = 5,
  max_targets = 10,
  report = FALSE
)
}
