% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_cohort_data.R
\name{fetch_cohort_data}
\alias{fetch_cohort_data}
\title{Fetch and preprocess TCGA miRNA + RNA + clinical data for any cancer type}
\usage{
fetch_cohort_data(
  cancer_type,
  save_dir = file.path(tempdir(), cancer_type),
  min_os_days = 1,
  log2_pseudocount = 1,
  min_mirna_cpm = 1,
  min_gene_cpm = 1,
  min_mirna_prev = 0.2,
  min_gene_prev = 0.2,
  verbose = TRUE
)
}
\arguments{
\item{cancer_type}{Character. TCGA project code, e.g. "TCGA-PAAD", "TCGA-BRCA".}

\item{save_dir}{Character. Directory to cache downloaded GDC files.
Defaults to a subdirectory named after the cancer type inside tempdir().}

\item{min_os_days}{Numeric. Minimum OS_days to retain a patient (default 1).}

\item{log2_pseudocount}{Numeric. Pseudocount before log2 transform (default 1).}

\item{min_mirna_cpm}{Numeric. Minimum CPM for miRNA filtering (default 1).}

\item{min_gene_cpm}{Numeric. Minimum CPM for RNA filtering (default 1).}

\item{min_mirna_prev}{Numeric. Minimum sample prevalence for miRNA (default 0.2).}

\item{min_gene_prev}{Numeric. Minimum sample prevalence for RNA (default 0.2).}

\item{verbose}{Logical. Print progress messages (default TRUE).}
}
\value{
A named list with:
\itemize{
\item \code{mirna_log}   - log2 miRNA matrix (miRNAs x patients)
\item \code{rna_log}     - log2 RNA matrix (genes x patients)
\item \code{clinical}    - data frame: patient, OS_days, OS_status, age, stage_clean
\item \code{mirna_norm_map} - data frame: original, norm
\item \code{cancer_type} - the input cancer_type string
\item \code{n_patients}  - number of patients with complete data
}
}
\description{
Downloads miRNA-seq, RNA-seq, and clinical data from TCGA via TCGAbiolinks,
preprocesses them into the matrix format expected by mirBottleneck functions,
and returns a list ready to pipe into run_mirBottleneck().
}
\examples{
\donttest{
cohort <- fetch_cohort_data("TCGA-LUAD", save_dir = "/tmp/tcga_cache")
str(cohort)
}
}
